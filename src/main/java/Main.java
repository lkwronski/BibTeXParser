import core.BitTexCore;
import core.ResolverArgument;

public class Main {
    public static void main(String[] args) {
        ResolverArgument resolverArgument = new ResolverArgument(args);
        BitTexCore bitTexCore = new BitTexCore(resolverArgument);
    }
}
